# 鑫元宝 New API 升级改动必看

> 2026-09-16 更新：本文后续内容为历史升级记录，旧路径/未部署状态不可直接作为当前事实。当前源码为 `/Users/lujihong/docker/www/鑫元宝/new-api`；生产发布配置为 `/data/xybcloud/releases/aicc-20260916/compose.json`。优先阅读父目录 `项目管理说明.md` 与本目录 `本轮发布验收记录.md`。

> **动手前必须读完本文。** 任何升级、合并官方代码、重建镜像、改 `SQL_DSN`、对生产库执行 `ALTER` 之前，先按本文核对。  
> 生产站点：**鑫元宝云计算模型服务平台** · 对外域名 **`https://api.xybcloud.com`**  
> 代码目录：`/Users/lujihong/docker/www/new-api`  
> 文档日期：2026-09-09  
> **本文不含任何数据库密码、Redis 密码、Session 密钥。** 这些只存在于本机 `docker-compose.yml` / `.env`，禁止写进 Git 与文档。

---

## 0. 先看结论（当前真实状态）

截至本文更新时：**生产数据库仍未迁移、生产容器仍未用新镜像启动。**  
本地已经具备 overlay 脚本和部署交接文档，**不等于线上已升级。**

| 项 | 事实 |
|---|---|
| 本地 HEAD（未合并官方前） | `aca72e78d`，`git describe` ≈ `v1.0.0-rc.24-35-gaca72e78d` |
| 官方最新标签 | `v1.0.0-rc.36`（worktree：`/Users/lujihong/docker/www/new-api-rc36`） |
| **正确升级目标** | `upstream/main`，不要停在裸 rc.36。标签之后至少还有：`fix(options): rebuild options table primary key...`、`fix: preserve provider fields in Sora video queries` |
| 落后官方 | 约 93 个提交 |
| 本机相对官方的独有提交 | **仅 1 个**：`aca72e78d`（已拆到 `site-overlay/`，禁止 cherry-pick 整提交） |
| `site-overlay/` + `scripts/` | 品牌覆盖、i18n 增量、Go 补丁快照、apply/upgrade/preflight 脚本、Dockerfile 已挂钩 |
| 数据库 | **仍未** dump、未查询、未 ALTER。迁移发生在新 master 进程首次连生产 DSN 时 |
| 容器 | compose 中 Cookie 域名已写成 `https://api.xybcloud.com`，**未重建容器则进程仍是旧值** |
| 部署手册 | **`交接给部署AI.md`**（给另一名 AI 上服务器用） |

合并官方后默认执行 `scripts/upgrade-from-upstream.sh --mode=branding`。`--mode=full` 含旧定价页与 Go 补丁，打不上必须停。

---

## 1. 升级铁律（违反即停）

1. **同一条 `SQL_DSN`，同一套 PostgreSQL 库 `newapi`。** 禁止换库名、禁止指向空库、禁止把官方示例 SQLite 当生产。换 DSN 等于把注册用户丢掉。
2. **禁止 `DROP DATABASE` / `DROP TABLE` / `TRUNCATE`。** GORM `AutoMigrate` 是加表加列，不是重建。
3. **禁止 `docker pull calciumion/new-api:latest` 覆盖本站点镜像。** 官方镜像没有鑫元宝品牌、页脚、首页、邮件模板。生产必须 **本地 `build` 后打标签** 再启动。
4. **禁止用官方 `docker-compose.yml` 覆盖本机 compose。** 里面有 DSN、Redis、Session、域名。合并 upstream 时必须 `git checkout HEAD -- docker-compose.yml`（或升级前先拷走）。
5. **禁止覆盖、删除、提交：** `.env`、`data/`、`logs/`、备份文件、密钥。
6. **禁止整提交 cherry-pick `aca72e78d`。** 它会把官方已重写的 `video_proxy.go`、定价页、锁文件一起盖掉。
7. **禁止把 `web/pnpm-lock.yaml`、空的根目录 `VERSION` 覆盖到新版本。** 官方前端用 bun；空 `VERSION` 会让进程显示 `v0.0.0`。
8. **禁止生产使用 `SKIP_64BIT_QUOTA_SCHEMA_CHECK=true`。** 额度列不是 64 位时，官方会拒绝启动——这是保护，不是障碍。
9. **先备份，再动库，再换镜像。** 没有可用的 `pg_dump` 不准启动新二进制连生产库。
10. **先演练库，后生产库。** 用 dump 恢复出的副本跑完 AutoMigrate 和冒烟，再对生产做同一件事。

---

## 2. 数据会不会丢？迁移到底做什么

New API **没有编号 SQL 迁移文件**（没有 `001_xxx.sql` 那种版本表）。

主节点启动时：

1. 连上 `SQL_DSN` 指向的库（生产是 PostgreSQL）。
2. **rc.36 起**先跑硬门槛：`ensureUserQuotaColumns`。`users.quota / used_quota / aff_quota / aff_history` 若仍是 32 位 `integer`，**进程直接退出，不会迁表。**
3. 再跑若干针对性迁移（见 §3），然后 GORM **`AutoMigrate`**：只 **CREATE TABLE / ADD COLUMN**，按模型补齐。
4. **不会**清空 `users`、`tokens`、`logs`、`channels`、`options`、`topups`、`redemptions`、任务表。
5. 从节点（非 master）**不跑** AutoMigrate。

`options` 表的加载顺序：

1. 内存写入代码默认值（含 `SystemName` 默认值）。
2. **`loadOptionsFromDatabase()` 用库里的行覆盖内存。**
3. 因此后台已经保存过的站名、Logo、首页 HTML、支付、`ModelRatio`、`ServerAddress` **会保留**，不会因为二进制默认值是 `New API` 就改掉生产站名。

**但是：**

- 代码里的默认站名只在 **库里没有对应 key** 时才会写进去。
- 对外链接、重置密码、OAuth、视频代理 URL、支付 Webhook 走的是库里的 **`ServerAddress`**，不是 compose 里的 `FRONTEND_BASE_URL`。
- **主节点会忽略 `FRONTEND_BASE_URL`**（`router/main.go`）。单机部署改这项几乎无效。
- 真正管登录 Cookie Origin 的是 **`SESSION_COOKIE_TRUSTED_URL`**（`SESSION_COOKIE_SECURE=true` 时必填，精确 HTTPS Origin，逗号分隔，不要路径、不要通配符）。

**结论：** AutoMigrate 保护的是「表还在、行还在」。它 **不保护** 计费语义。从 rc.26 及更早升上来，视频模型旧 `ModelRatio` **不会**自动变成新的任务计费表达式，必须在后台重配，否则 Seedance 等任务会按错误规则扣费。

---

## 3. 升级时库结构会发生什么（rc.24 → `upstream/main`）

以下基于官方 `v1.0.0-rc.36` 及之后的 `upstream/main`。演练时以实际启动日志为准。

### 3.1 启动前硬门槛（失败则新进程起不来，旧数据原封不动）

**用户额度列必须已是 64 位。** PostgreSQL 要求类型为 `bigint` / `int8`。

官方 **不会** 自动把 `integer` 升成 `bigint`。必须运维显式 `ALTER`。当前本地结构体仍写着 `gorm:"type:int"`，所以 **不能指望 AutoMigrate 帮忙改宽度**。

预检（只读）：

```sql
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name = 'users'
  AND column_name IN ('quota', 'used_quota', 'aff_quota', 'aff_history');
```

若是 `integer`，**先全库备份**，再在维护窗口执行（有表锁，选低峰）：

```sql
ALTER TABLE users
  ALTER COLUMN quota TYPE bigint USING quota::bigint,
  ALTER COLUMN used_quota TYPE bigint USING used_quota::bigint,
  ALTER COLUMN aff_quota TYPE bigint USING aff_quota::bigint,
  ALTER COLUMN aff_history TYPE bigint USING aff_history::bigint;
```

改完再查一次，确认四列都是 `bigint`，然后才允许新版本启动。

### 3.2 `tokens.key` 唯一性（PostgreSQL）

`migrateTokenKeyUniqueness` 会把旧的 UNIQUE constraint 收成独立 unique index，期间 **`LOCK TABLE tokens`**。

若存在重复 `key`，迁移会失败，服务起不来。预检：

```sql
SELECT key, COUNT(*) AS n
FROM tokens
GROUP BY key
HAVING COUNT(*) > 1;
```

有结果就必须先人工处理重复令牌，**禁止跳过**。

### 3.3 `prefill_groups` 唯一性

`migratePrefillGroupUniqueness` 同样会锁表、整理历史 GORM 留下的唯一约束。未知冲突对象会报错停机，不会瞎删。

### 3.4 `options` 主键（必须用 `upstream/main`，不要停在裸 rc.36）

提交 `4fc9d1f1f`：`migrateOptionPrimaryKey`

- 部分长寿命库的 `options` **没有** 在 `key` 上的 PRIMARY KEY。
- 写定价时 `ON CONFLICT DO NOTHING` 可能插出重复默认行，读的时候把真实定价盖掉，界面显示保存成功、多节点却丢配置。
- 迁移：检测缺失唯一键 → 加锁 → 按 key 去重（last-wins）→ 重建带主键的表并原子替换。旧行保留在 `options_legacy_<时间戳>`。失败则报错，不把半成品留下当正式表。

**所以目标提交必须包含这一笔。** 只升到 rc.36 标签，生产定价有丢配置风险。

预检（只读）：

```sql
SELECT kcu.column_name, tc.constraint_type
FROM information_schema.table_constraints tc
JOIN information_schema.key_column_usage kcu
  ON tc.constraint_name = kcu.constraint_name
 AND tc.table_schema = kcu.table_schema
WHERE tc.table_schema = 'public'
  AND tc.table_name = 'options'
  AND tc.constraint_type IN ('PRIMARY KEY', 'UNIQUE');

SELECT key, COUNT(*) AS n
FROM options
GROUP BY key
HAVING COUNT(*) > 1;
```

### 3.5 其它列级迁移（AutoMigrate 前/中）

- 订阅计划 `price_amount`：float/double → decimal（已存在列时的兼容）。
- `tokens.model_limits`：varchar → text。

这些是加宽/改类型兼容，不是清数据。仍必须先有 dump。

### 3.6 AutoMigrate 会新建或补齐的表（相对当前 rc.24 线，重点新增）

现有用户相关表都会继续 `AutoMigrate`（加列不加行清理），包括但不限于：`users`、`tokens`、`user_sessions`、`channels`、`abilities`、`logs`、`options`、`top_ups`、`tasks`、`redemptions`、`quota_data` 等。

**相对当前生产代码，升级后会多出来的典型对象：**

| 对象 | 作用 | 对老数据 |
|---|---|---|
| `task_plugins` | 任务插件（豆包 Seedance 等改为插件） | 新表，空表起步；**type=54 渠道不用改成 type 61**，启动后会映射到插件 key `doubao` |
| `login_encryption_keys` | 浏览器登录加密材料 | 新表，首次启动生成，与用户密码哈希不是同一件事 |
| `audit_logs` | 审计日志（走日志库；若 `LOG_SQL_DSN` 为空则与主库相同） | 新表，不影响用量日志 |
| 任务表新列 | 如插件快照字段 | 加列，旧任务行保留 |

从节点不迁表。多实例时必须先起 **master** 完成迁移，再起其它节点。

### 3.7 启动后会改内存、可能写回的配置

- `InitializeUserAuthVersions` / `InitializeExternalIdentityClaims`：给缺字段的用户补认证版本等，不是删用户。
- 任务插件默认启用；工厂插件 `doubao` 会注册。后台若关了 `TaskPluginEnabled`，以库为准。

### 3.8 AutoMigrate **不会**做的事

- 不会把旧 `ModelRatio = 46` 变成 Seedance 任务表达式。
- 不会改 `ServerAddress`、`SystemName`、支付密钥。
- 不会迁移或删除 `data/` 卷里的文件。
- 不会改 Redis 里的会话（换 `SESSION_SECRET` 才会让所有人掉线——**禁止无故改 Secret**）。

---

## 4. 域名、Cookie、站点配置（和代码默认值不是一回事）

生产对外 Origin：**`https://api.xybcloud.com`**（公司官网 `https://www.xybcloud.com` 不要改成 API 域）。

| 配置 | 在哪 | 作用 | 升级注意 |
|---|---|---|---|
| `SESSION_COOKIE_TRUSTED_URL` | compose 环境变量 | Secure Cookie 下 refresh/logout 的可信 Origin | 必须是 `https://api.xybcloud.com`。若旧域 `token.profitly.cn` 仍解析到本站，过渡期写成两个 Origin，逗号分隔，否则旧域登录刷新会 403 |
| `SESSION_COOKIE_SECURE` | compose | 必须 `true`（HTTPS 站点） | 不要改 false |
| `FRONTEND_BASE_URL` | compose | **主节点忽略** | 单机改了也不决定面板域名 |
| `NODE_NAME` | compose | 实例身份、任务归属日志 | **不要随便改。** 一改，`system_instances` 会多一条新节点，旧名变过期。与用户额度无关 |
| `ServerAddress` | **数据库 `options`** | 邮件链接、OAuth 回调、视频 `/v1/videos/{id}/content`、支付 Webhook | 代码改了 compose **不会**自动改这项。必须在后台「系统设置 → 服务器地址」核对为 `https://api.xybcloud.com`（无尾斜杠或按现网习惯统一） |
| `SystemName` / `Logo` / `HomePageContent` / `Footer` | `options` | 前台展示 | 以库为准。源码默认「鑫元宝云计算模型服务平台」只在库空时出现 |
| Passkey RPID | 可由 `ServerAddress` 推导 | 域名变了 Passkey 会失效 | 改域后要在 Passkey 设置里对齐 |

升级后立刻在后台只读核对（不要在文档或聊天里粘贴密钥）：

```sql
SELECT key, value
FROM options
WHERE key IN (
  'SystemName', 'ServerAddress', 'Logo', 'HomePageContent',
  'TaskPluginEnabled', 'ModelRatio', 'ModelPrice'
);
```

`HomePageContent` / `ModelRatio` 可能很长，导出到文件看，不要在生产从客户端乱改。

---

## 5. 站点定制：必须保留 vs 禁止整文件覆盖

本地相对官方的差异几乎全在独有提交 `aca72e78d`。正确做法是 **overlay + 补丁**，不是 cherry-pick 整提交。

当前 `site-overlay/` 已抽出文件和补丁，但 **还不能自动应用**。每次升级仍须按下面分类人工（或等脚本完成后再用脚本）。

### 5.1 必须精确保留（覆盖官方同名文件即可）

- `custom_homepage.html`、`public/**` 品牌资源
- `web/public/` 下 logo / favicon / 鑫元宝图标
- `web/index.html` 标题与描述
- `web/src/lib/constants.ts` 的 `DEFAULT_SYSTEM_NAME`
- `web/src/main.tsx` 缺省站名
- 布局：`footer.tsx`、`header-logo.tsx`、`mobile-drawer.tsx`、`system-brand.tsx`
- 登录注册：`web/src/features/auth/**`
- 首页、错误页：`home/index.tsx`、`errors/general-error.tsx`
- `common/constants.go` **只保留** `SystemName = "鑫元宝云计算模型服务平台"`，不要整文件覆盖（会丢掉官方新常量）
- 邮件模板：`controller/misc.go` 里鑫元宝验证/重置信（应用 **hunk 补丁**，不要整文件替换）

i18n **禁止**用整份 `zh.json` 覆盖官方文件（会丢掉新 key）。只合并这些定制句：

- `Sign out` →「退出」；`Signed out` →「已退出」
- 视频预览相关：`Video Preview`、`Video playback failed`、`Preview generated video` 等

### 5.2 禁止整文件盖到新版本上（会毁掉官方新能力）

| 文件 | 原因 |
|---|---|
| `controller/video_proxy.go` | 官方已是完整签名 `?access=` 代理（约 600+ 行）。本地旧文件约 200 行，且曾放行「task_id 足够长则匿名可播」。整文件覆盖等于拆掉官方鉴权 |
| `web/src/features/system-settings/models/model-pricing-sheet.tsx` 及配套 pricing 输入 | rc.36 是任务表达式定价 UI。用 rc.24 页盖上去，后台无法配 Seedance 表达式 |
| `web/src/features/usage-logs/components/columns/task-logs-columns.tsx` 等 | 官方有 `TaskArtifactsCell`。本地预览对话框要 **rebase 进新组件**，不能整页替换 |
| `web/pnpm-lock.yaml`、`go.mod` 整文件 | 依赖以官方为准，再按需 `go mod` / `bun install` |
| 根目录空 `VERSION` | 构建前用 `git describe --tags --always` 写入 |
| 官方 `docker-compose.yml` | 永远不用 |

### 5.3 后端补丁（三路合并，失败必须停）

已抽出（相对 `e2c7aa7b1..aca72e78d`），**不能直接打到 rc.36 上**，大概率 reject，需要以新文件为基底重做：

- `site-overlay/patches/001-common-branding-and-misc.diff`
- `site-overlay/patches/002-video-proxy-and-auth.diff`
- `site-overlay/patches/003-topup-quota-and-task.diff`
- `site-overlay/patches/004-relay-custom.diff`

产品取舍必须升级时明确写进发布记录：

- **匿名已完成任务视频流**：本地有，官方改为签名 `access`。保留本地行为要在新 `video_proxy` / `middleware` 上重打补丁，并接受安全面变大。
- **充值上限**：官方已有 `MaxWalletQuota` / `getMaxTopUpAmount`。不要把旧 `topup.go` 整文件盖回去。
- **`GenerateDefaultToken` 默认 true**：本地 `common/init.go` 改过默认值。升级后核对环境变量，避免每个新用户自动发卡与预期不符。

### 5.4 品牌配置单一来源

`site-overlay/branding.json`：

- `systemName`：鑫元宝云计算模型服务平台
- `siteUrl`：`https://api.xybcloud.com`

不要再增加「默认等于 New API」这类易误解字段。运行时仍以数据库 `options.SystemName` 为准。

---

## 6. Seedance / 视频计费（升级后必重配，否则会错扣费）

### 当前生产（rc.24 线）

- 渠道类型 **Doubao Video `type=54`**
- 模型在 `relay/channel/task/doubao`：`doubao-seedance-2-0-260128`、`doubao-seedance-2-0-fast-260128`
- 硬编码分辨率单价表；结算走 token `total_tokens` + `ModelRatio`
- 默认 `ModelRatio` 映射 **不含** 这些模型，后台必须已手动配过（例如 46 / 37）才正确
- `/v1/chat/completions` **不会**走这条视频价表

### 升级后（插件 `plugins/tasks/doubao`）

- 额外模型：`doubao-seedance-2-0-mini-260615`、`doubao-seedance-2-5-260628`
- `usageSchema`：`tokens`、`resolution`（480p/720p/1080p/4k）、`video_input`
- **type=54 会自动映射插件 key `doubao`，不必改渠道类型**
- 旧 ModelRatio **不会**变成表达式。必须在「模型定价 / 任务计费表达式」里按分辨率 × 是否视频输入重配
- 插件内部仍有 `videoInputRatio` 硬编码参考值（2.0：46/28/51/31 等，单位与后台表达式不是自动同步）

升级后用测试令牌跑一条 **真实会完成的短视频**（小分辨率），核对扣费与上游账单，再开放给用户。

---

## 7. 镜像与进程版本

- compose 里即使写着 `image: calciumion/new-api:latest`，本站也必须改为 **build 本地 Dockerfile**（或显式本地 tag），否则下次 `compose pull` 会把品牌冲掉。
- 构建前写入 `VERSION`：`git describe --tags --always`（不要提交密钥，VERSION 本身可以进镜像）。
- 官方 Docker 前端阶段用 **bun**，不要用 pnpm lock 去 `bun install --frozen-lockfile`。
- 根目录 `VERSION` 若为空，日志会变成 `v0.0.0`，和 rc.24 对不上，排障会误判。

---

## 8. 标准升级流程（每次都走，不许跳步）

### A. 只读预检（生产库，不改数据）

在 **SQL 客户端用只读账号** 或维护窗口只读执行 §3 的预检 SQL，保存结果到 `backups/`（该目录不要提交 Git）：

1. 额度列类型
2. `tokens.key` 重复
3. `options` 主键 / 重复 key
4. 导出 `SystemName`、`ServerAddress`、渠道 type=54 列表、相关模型名

同时记录：当前容器镜像 ID、`git rev-parse HEAD`、compose 中 **非密钥** 环境变量名（不要把 DSN 整串贴到聊天或文档）。

### B. 全库备份

```bash
# 在能访问 Postgres 的机器上执行；连接参数用环境变量，不要写进仓库
pg_dump --format=custom --file="backups/newapi-$(date +%Y%m%d-%H%M%S).dump" \
  --dbname="$SQL_DSN"
```

确认 dump 文件非空，并 **另存一份离线副本**。没有恢复演练的 dump 不算备份。

### C. 演练库（强制）

1. 新建空库或另一套 Postgres，`pg_restore` 刚才的 dump。
2. 从 `upstream/main` 拉代码到独立 worktree（不要直接在正在跑生产的工作树上乱合并）。
3. 保留本机 `docker-compose.yml`，DSN **指向演练库**。
4. 按 §5 叠品牌与补丁，构建镜像，启动 **仅这一套**。
5. 看启动日志：`database migration started` 是否成功；有无 quota 32-bit / token unique / options PK 报错。
6. 冒烟：管理员登录、老用户登录、令牌调用、type=54 视频任务、充值页打开、页脚 ICP、站名。
7. 再查演练库：用户数、令牌数与备份一致；新表 `task_plugins` 存在。

演练失败 → **禁止**对生产做同样操作。用 dump 证明可以整库回滚。

### D. 生产窗口

1. 再 dump 一次（窗口开始时刻）。
2. 通知用户短暂停服或只读（按业务需要）。
3. 停旧容器（不要删 `data/`、`logs/` 卷）。
4. 若额度仍是 `integer`：先 ALTER（§3.1），再启动新进程。
5. 合并/检出 `upstream/main` + overlay，**compose 仍用本机文件**。
6. `docker compose build && docker compose up -d`（本地镜像，不许 pull 官方 latest）。
7. 只起 master，等健康检查与迁移日志结束。
8. 后台核对 `ServerAddress`、`SystemName`，重配 Seedance 表达式，跑一条测试视频。
9. 观察错误日志、扣费、登录 Cookie（必须用 `https://api.xybcloud.com` 访问）。

### E. 失败回滚

1. 停新容器。
2. 启动 **旧镜像**（事先记下 image id）+ **未改过的 compose**。
3. 若新进程已经 AutoMigrate 加了列：通常旧二进制仍能读多出来的列；若旧进程因未知列/约束起不来，再用 dump **恢复到升级前**（这会丢掉升级窗口内的新订单/新注册，所以窗口要短，并提前公告）。
4. `options_legacy_*` 表不要随便 DROP，直到确认定价正常。

---

## 9. 合并官方代码时的 Git 做法

```text
git fetch upstream
git merge --no-commit --no-ff upstream/main
# 立刻找回生产 compose，禁止把官方示例 compose 带进来
git checkout HEAD -- docker-compose.yml
# 若有 .env：同样 checkout HEAD
# 冲突只解决业务代码；site-overlay/ 保持我方文件
```

不要：

- `git cherry-pick aca72e78d`
- `git reset --hard` 生产库所在机器上未提交的 compose
- 把 `site-overlay/patches` 在未 rebase 的情况下 `git apply` 到 rc.36 并强行 `--reject` 后带着 `.rej` 上线

补丁打不上就 **停**，在新文件上重做定制，再把成功后的文件拷回 `site-overlay/files/`、把新 diff 存进 `patches/`，供下次升级使用。

---

## 10. 升级后验收清单（全部勾完才算成功）

- [ ] 启动日志版本号 = `git describe`，不是 `v0.0.0`
- [ ] 健康检查 `/api/status` 成功
- [ ] 老用户能登录；Cookie 在 `https://api.xybcloud.com` 下可 refresh
- [ ] 用户数、令牌数、余额抽查与升级前 dump 一致
- [ ] 页脚公司名、ICP、站名正确
- [ ] `options.ServerAddress` 为 `https://api.xybcloud.com`
- [ ] type=54 渠道仍在且能提交 Seedance；表达式定价已重配；测试任务扣费正确
- [ ] 视频播放按产品决策工作（签名 `access` 或已 rebase 的匿名补丁）
- [ ] 充值页、支付回调 URL 仍指向本域
- [ ] 没有误改 `SESSION_SECRET`、DSN、Redis
- [ ] `NODE_NAME` 与升级前一致（除非有意改实例名并清理旧实例记录）
- [ ] dump 文件已离线保存

---

## 11. 以后每一次升级都要重复的最短清单

1. 读本文（若官方又加了新的启动门槛，先补进 §3）。
2. dump。
3. 只读预检 SQL。
4. 演练库升级。
5. overlay / 补丁，编译失败就停。
6. 生产短窗口：ALTER（仅当预检需要）→ 本地构建 → master 迁移 → 定价与域名核对。
7. 把这次新产生的定制写回 `site-overlay/`，补丁相对 **新的 upstream 基底** 重新导出。

---

## 12. 本地脚本已就绪 vs 服务器仍未做

本地已具备：`scripts/apply-site-overlay.sh`、`upgrade-from-upstream.sh`、`preflight-db.sh`、`write-version.sh`、Dockerfile overlay、`docker-compose.site-build.yml`、`交接给部署AI.md`。

**服务器上仍未做（必须由部署 AI 执行）：**

- `pg_dump` 与演练库恢复
- 额度列 / token 重复 / options 主键只读预检
- 合并 `upstream/main` 并启动新 master（真正的 AutoMigrate）
- 重建容器使 `SESSION_COOKIE_TRUSTED_URL=https://api.xybcloud.com` 生效
- 后台核对 `ServerAddress`、重配 Seedance 表达式

Go 补丁仍是 rc.24 基底：合并官方后先 `--mode=branding`。`--mode=full` 打不上就停，rebase 后再 `refresh-site-overlay.sh`。

`NODE_NAME` 本机 compose 已回到 `token-profitly-cn`。以生产 `docker inspect` 为准，不要无故改实例名。
