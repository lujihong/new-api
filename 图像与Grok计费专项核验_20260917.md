# 图像与 Grok 计费专项核验

状态：实施与验证中，尚未发布。本文替代此前关于本范围“已全部精确”的宽泛结论，不代表整站财务验收。

## 生产核查（只读）

- 网关运行 xyb-new-api:rc37-20260917-r3；工作台运行 xyb-workbench:discount-20260917-r7。
- 四个目标模型位于启用的渠道 5（type 60），模型映射为空。该渠道连接二级中转，而不是直接请求官方；官方协议和本站供应商协议必须分别验证。
- gpt-image-2：管理员覆盖式 `tier("base", p * 5 + img_o * 30 + img * 8 + cr * 1.25)`。
- gpt-image-2-1k：按次价格 0.04，无独立表达式；不得由名字推断为官方型号或官方按张价格。
- grok-imagine-video：480 分支每秒 0.05，其余 720 分支每秒 0.07。
- grok-imagine-video-1.5：1080 分支每秒 0.25，480 分支 0.08，其余 720 分支 0.14。
- Grok 表达式使用 `param("resolution")`，时长 `vs == 0 ? 5 : vs`。
- 本次查询目标模型消费汇总：gpt-image-2 19 条，gpt-image-2-1k 31 条；两个 Grok 型号未查询到匹配的消费记录。不要把日志条数当生成次数。
- 图像消费日志 1421：`/v1/images/generations`，动态计费 base，组倍率 1，prompt_tokens=1，completion_tokens=0，quota=3。源码 ImageHelper 会将零输入/总用量改为 1；现有日志不能区分上游本来返回 1 与本地补 1。不能据此恢复真实用量或推算历史应收金额。

## 官方证据

### xAI

来源：https://docs.x.ai/developers/pricing（抓取页标注 2026-09-14）。页面明确全部金额为 USD。

| 型号 | 输出 480p/秒 | 输出 720p/秒 | 输出 1080p/秒 | 媒体输入 |
| --- | ---: | ---: | ---: | --- |
| grok-imagine-video | $0.05 | $0.07 | 表中未列 | 视频 $0.01/秒；图像 $0.002/张 |
| grok-imagine-video-1.5 | $0.08 | $0.14 | $0.25 | 图像 $0.01/张 |

输出单价的数字与当前配置一致，不代表本站人民币零售价、供应商采购成本和官网美元价相同；媒体输入费用不应在利润核算时遗漏，但是否对客户另收属于商业策略，未擅改。

来源：https://docs.x.ai/developers/model-capabilities/video/generation（抓取页标注 2026-09-08）。

- 官方创建：POST `/v1/videos/generations`；返回 request_id。
- 官方查询：GET `/v1/videos/{request_id}`；状态 pending/done/expired/failed。
- 完成示例在 video.duration 给出时长，video.url 给出临时资源。
- 可选 duration 为 1–15 秒，默认分辨率 480p。不要将本站 5 秒价格兜底认作官方默认生成时长。
- 1.5 文生/图生支持 1080p，参考生视频上限 720p。
- 编辑/扩展有独立端点，不能直接套标准文生请求。
- 网关 Sora 兼容插件用 `/v1/videos`、id/task_id 和顶层 duration/seconds；二级供应商可能已经转换协议。不能由官方路径不同直接判本站接口不可用，也不能未经实际响应确认宣称完全兼容。

### OpenAI

型号页：https://developers.openai.com/api/docs/models/gpt-image-2

确认 gpt-image-2 与 gpt-image-2-2026-04-21；没有取得 gpt-image-2-1k 是官方型号的证据。

指南：https://developers.openai.com/api/docs/guides/image-generation

主会话直接读取 HTML 的 Cost and latency / Earlier GPT Image models 段落核对：

- 计算器段落明确 GPT Image 2.5 和 GPT Image 2 共享图像输出 token 单价，并显示每百万图像输出 token 30 USD。这是 GPT Image 2 输出 30 的直接官网证据，不是借用相似型号推断。
- GPT Image 2 的 1024×1024 输出示例：low $0.006，medium $0.053，high $0.211；1024×1536 与 1536×1024 示例分别 $0.005/$0.041/$0.165。仅为输出费用，不含文本与参考图输入，不能按固定 1K 价格等同全部质量。
- 当前官网通用定价页主要展示 2.5；GPT Image 2 专属文本输入/缓存、图像输入/缓存 token 单价仍未完成独立核验。本站现有数字不作为官网证据，不擅自更改。

## 实测缺陷与实现

### Grok 分辨率

旧任务入口仅把 Usage facts 交给表达式，param 只能读取 Body，因此缺 Body 时恒走末尾 720p 分支。主会话在 pkg/billingexpr/task_usage_test.go 使用两个型号、5/8 秒、多分辨率复现；金额没有额外除以百万。

实现：可选适配器接口提取六个白名单标量（resolution/resolution_name/size/seconds/duration/aspect_ratio），独立保存 TaskRequestParams；预扣读取这些参数，完成结算恢复冻结参数，实际 seconds 用量仍能更新；旧快照不回填，不猜测历史分辨率、不追扣历史账单。尚需审查跨插件兼容与真实协议 decoder 路径。

### Images 输出

旧 Images 归一化将 output_tokens 只写入 CompletionTokens（c），没有填入 img_o 所需图像明细，因而线上 img_o 表达式漏计标准响应输出。

实现：仅在 Images 专用处理路径，通过原始 JSON 检查 completion_tokens_details 的存在性。没有该字段才将 output_tokens 映射为图像输出；显式空对象、零值及供应商混合分项保持。没有修改通用 DTO 或把聊天输出当图片。

合成向量 input text100/image200、output1000，原式金额由 0.0021 修为 0.0321（配置计费单位，未含组倍率）。另测折扣 0/0.5/1、generations/edits 的 JSON/SSE/JSON-as-SSE路径。

仍未解决：无 usage 时无法知道真实用量；旧表达式缺 img_cr 导致合法缓存图像明细的计费重叠；不能把已修输出映射当成全部图像价格准确。

### 展示与折扣编辑器

- 价格卡片显示图像输入、输出、缓存等实际配置分项；Grok 独立按秒只读投影，未知表达式不猜价，源码移到可展开审计项。
- 折扣编辑器增加编辑、免费确认、每页25条、10,000条有限DOM、保存/刷新互斥、后台刷新错误保留草稿、sid隔离，修正9.5/8.5折与100%沿用组价说明。
- 尚未完成浏览器视觉/键盘/移动端真机验收。source-overlay 尚未同步，严禁据本地构建宣布线上已更新。

## 主会话验证记录

- Images 新增和已有目标测试通过（最新主会话复跑1.510s）。
- pkg/billingexpr、relay、jsplugin、service 的新增参数快照与零倍率目标回归通过；正则带后缀，确认没有将 no tests to run 当通过。
- 折扣组件10例通过，包括保存锁、万条分页、免费确认、后台刷新失败及同账号sid切换晚返回。
- 价格卡片31例通过。前端tsgo通过，目标oxlint 0警告/0错误，rsbuild生产构建通过。
- 曾有过宽/过窄测试正则出现 no tests to run；已重跑准确名称，不能将早期空跑计入覆盖。
- 独立verification审查进行中；尚无最终发布许可结论。

## 未完成发布门槛

1. 独立审查 findings 收口与完整回归；真实上游协议/usage样本与缺usage策略。
2. GPT Image 2 输入/缓存价格核验、供应商规格与采购证据。
3. 工作台 resolution_name 与网关 resolution 的实际协议一致性，不可只修 API 手工请求。
4. 精确同步 overlay，真实浏览器桌面/移动/深浅色验收。
5. 生产备份、候选构建、可回退发布与线上验证；当前未执行。
6. 财务待办92中的终态CAS/资金/日志/缓存恢复、采购账本完整性仍独立未完成。
