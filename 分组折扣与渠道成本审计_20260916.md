# 分组折扣、渠道成本和利润审计（2026-09-16）

状态：关键源码已核验；有效任务倍率快照重算修复已随r5发布，并通过远端Snapshot|Recalculate专项。未改生产价格、增加采购价或宣称全模型精确。逐客户模型优惠与成本账本仍待实施。

## 逐模型优惠实现（discount-r1已发布，规则仍为空）
- 最终负零canonical及controller409专项远端通过后发布。线上管理卡片/精确模型目录GET200、空规则原价、未编辑保存禁用，artifact://f748638d-2e96-478d-9f87-9f8a947b4d91；模型广场详情只读正常artifact://4823b6ae-4468-47e0-9997-b6bbb27124ec。不采用工具混入历史聊天的架构建议作新用户指令。
- 工作台r1构建因内存SIGKILL失败数次，最终1CPU/2GB物理+6GB总内存、cpus1/webpackBuildWorker false/webpackMemoryOptimizations、Node堆1400通过并发布，双站健康200/零重启。首次工作台线上本人报价暂不可用，发现video及canvas生成面板未主动订阅，已补3处hook、本地新增真实React测试通过(报价8项)，r2发布后发现/api/model-pricing公开路由漏UserAuth，补真实JWT两个入口矩阵(匿名401/专属身份200)后r3发布并闭合。线上Seedance显示按实际用量结算(artifact://9be943d4-e9fd-4a10-b361-0110cd5e83d0)，服务器日志确证GET200；browser关于RSC无请求的推测不采用。
- 本人报价目前以USD基价+最终group倍率保守估算，表达式按实际用量，points_cost=null；CNY展示口径/实际积分仍需进一步对齐，不声称完整费用审计。

### 实现与验证过程（下述候选状态为历史记录）
- 单原子Option ModelDiscountRules={groups,users}，个人优先组再1，精确OriginModelName，不改基础价。规则不可变快照首次定价捕获，重试复用，最终倍率=原有效组倍率×factor，零合法。
- 模型/用户/组引用校验、事务成功后发布；周期重载与同进程写排序。控制台保存携带expected_value，DB行锁内比较严格canonical，冲突409保留草稿；已补负零/空owner一致性。精确模型目录来自渠道声明，不再用内置模型目录。
- 普通/按次/任务/Realtime接入最终倍率，任务持久version1折扣来源；违规费使用折前基数。
- 本人pricing响应独立map，不改共享cache；登录身份读取失败503，前端query按user+session、迟到响应拒收。group=all与0倍率保留。
- 工作台新增/pricing/self专属token桥接，禁redirect/共享cache。前端按精确模型ID、当前会话取消与清理。只USD/次、USD/千Token及分组倍率估算；tiered表达式不当免费，未经核实points_cost=null。
- 主会话核实旧工作台口径冲突：newapi_bridge.go577钱包quota/quotaPerUnit当人民币后×10积分；931流水另×10人民币再×10积分；公共status实读quota_per_unit500000/usd_exchange_rate1/quota_display_type CNY/price1。未擅改这些既有金额，后续需按充值/真实扣费账单对齐。
- 远端后端多轮TestModelDiscount/任务快照/AICC通过；工作台service/handler/router全回归通过；前端价格模块139+增补11项通过、工作台本人报价7项通过/双TS。最后canonical负零修复与controller409测试正final-gateway-test构建，未部署。
- 隔离目录/data/xybcloud/builds/model-discount-20260916，候选tags discount-20260916-r1。构建器限1600MiB/1核，专用build.swap2GiB，完成后停止并在内存足够时撤销。生产仍网关aicc-r5/工作台aicc-r4。

## 主会话直接核验的原有实现（优惠候选前）

1. 创建组入口：系统设置→计费→分组定价。`web/src/features/system-settings/models/group-ratio-visual-editor.tsx` 的 addRow 创建默认ratio=1的配置行；`ratio-settings-card.tsx` 的 saveGroupRatios 逐个调用 updateOption 保存 GroupRatio、GroupGroupRatio 等，不是新建独立组实体。
2. `setting/ratio_setting/group_ratio.go` 只有组倍率 map[string]float64、用户所属组→计费组 map[string]map[string]float64。第二层键是计费组，不是模型或用户ID。
3. `relay/helper/price.go` HandleGroupRatio 先确定 auto_group，再优先 GetGroupGroupRatio(UserGroup,UsingGroup)，不存在才使用 UsingGroup 的 GroupRatio。特殊倍率覆盖普通倍率，不是再乘一次。此函数未含用户ID×模型或用户组×模型规则。
4. 同文件 ModelPriceHelper 优先识别 tiered_expr，否则走模型单价/模型倍率；零售计费与实际选路渠道不是一回事。
5. `model/channel.go` 渠道有 Models、Group、Priority、Weight、ModelMapping、UsedQuota 等。`service/task_billing.go` LogTaskConsumption 用客户Quota累加 UpdateChannelUsedQuota，不能将这个数展示成采购成本。
6. `controller/relay.go` 在任务插入前保存 BillingContext：ModelPrice、GroupRatio、ModelRatio、OtherRatios、OriginModelName、PerCallBilling、TieredSnapshot；这为固定历史价格提供基础，但不能因此断言所有结算路径都使用快照。

## 真实金额展示核验（进行中）
- 已读网关model/topup.go：易支付成功按topUp.Money×QuotaPerUnit记账，正好与工作台wallet quota/QuotaPerUnit人民币口径对应。消费日志额外×10会把同额度显示放大10倍，不应再叠加在记录已包含的模型售价上。
- 流水先round两位积分再用points<=0判free，会把正quota小额标免费。只读生产聚合：865条消费、5条退款、21条quota>0且<250消费、0条type5且quota>0；未输出用户ID/密钥。
- 展示helper统一换算/小额格式和状态已实现，真实HTTP对照wallet/log相同quota完全一致；Type5有扣费保留，Type6成功差额退还/失败退款/未知仅退还。主会话补前端consumptionCostPresentation，去失败硬0/退款硬全退/重复正号/处理阶段假未扣费。parseErrorMessage也不推断已全退/未扣费。
- 工作台后端整体service/handler/router+vet+相关race通过；前端金额+个人价+上传+视频统一39项/TS通过。CNY依据严格读取/status，不猜7；USD基值保留。最终工作台discount-20260917-r6已发布且线上退款语义只读验收通过，未改基价或历史quota。

## 已核实风险与修复进度

### token重算价格漂移（有效快照已修，历史回退保留）
`service/task_billing.go` RecalculateTaskQuotaByTokens现在优先使用已保存且ModelRatio>0的ModelRatio/GroupRatio，GroupRatio=0合法；按次/表达式拒绝直接token回退、非法倍率拒绝、nil防护。新增task_billing_snapshot_test.go核验真实钱包/任务/用量/日志；修前出现预期200实际7000，修后远端Snapshot|Recalculate通过。缺快照或零模型倍率缺版本语义时保留原配置回退并告警，这并非已修全部历史账本。

### 最终用量与快照持久化
`service/task_polling.go` 先UpdateWithStatus保存终态，再在settleTaskBillingOnComplete中合并UsageFacts、修改EstimatedTier。`model/task.go` UpdateQuota仅更新quota列。由此最终facts不应假设已经持久化；等额结算在RecalculateTaskQuota提前返回，亦无新差额日志。需要明确最终账单状态及持久化，不以0差额代表无用量。

### 结算退款非原子
RefundTaskQuota及RecalculateTaskQuota分步变更资金、令牌、quota、统计和日志。已有错误告警但不是事务性幂等账本。持久化失败/重试/进程崩溃均需故障注入验证，不能宣称绝对不重扣/重退。

## 用户提出的目标尚缺实现

- 用户所属组×精确模型ID折扣、个人优先级和本人报价已发布；具体业务折扣值尚未由用户给出，规则空表不擅填；线上带折扣真实收费账单仍未主动生成验算。
- 渠道×模型×采购规则版本（币种、单位、条件、合同折扣、生效时间）。同名模型可以不同进价，不应改名或混改零售价。
- 按最终实际渠道、采购规则快照、真实用量生成成本；成本未知显示未知，不填0冒充免费。
- 独立收入、成本、退款与调整账本；区分扣额度、实际充值现金、赠送额度及现金退款，历史利润不可用今日配置追算。
- 内蒙古新上游所对应资产账号scope及路由固定规则。当前AICC全局凭证校验不等于每项资源持久绑定多上游账号。

## 后续验证准则

使用已确认合同价/官方价，不编造价格。逐模型记录接口、实际渠道、计量单位、配置版本、预扣、实际用量、最终扣费/退费、日志和前端金额。模拟通过、单个任务成功、构建成功、线上收费账单对齐分别报告，禁止混作全量验收。
