# 渠道采购成本与利润实现规格

状态：设计已基于源码审查，尚未实现。不得将此文档当已上线功能。

## 边界与原有机制

- 客户价格配置与采购成本分开；ModelDiscountRules只决定客户折扣，不能改渠道进价。
- Channel.UsedQuota累计客户消费，不是成本。Task.ChannelId/Properties/PrivateData已有最终渠道、模型、客户计价快照、上游任务ID和quota。
- model/ability.go GetChannel按可用组+模型筛选、priority降序级别、同级weight抽取；价格不是自动选路条件。渠道名或模型别名不能代替采购成本规则。
- model/main.go InitLogDB支持LOG_SQL_DSN独立日志库，不能假设主DB任务写和LOG_DB日志写可同事务。

## 新增主库账本

1. PurchasePriceVersion：channel_id、精确upstream_model、价格维度、币种(CNY/USD/明确其他)、decimal单价字符串/固定精度整数、计量单位、适用条件、生效时间、来源证据、发布状态。发布不可修改，只能新增版本。
2. UpstreamAttempt：每次实际上游发送单独attempt id/request id，真实channel/model，选中的采购价版本快照，submitted/known/unknown/not_applicable，最终规范usage、amount nullable、currency、原因、供应商请求/任务ID。
3. 成本unknown与known(0)必须区分；本地验证失败未发送可not_applicable，发送后超时/断线unknown，不能失败一律免成本。
4. 异步最终CAS赢家才发起本attempt的成本finalize，唯一键保护幂等；重试不同attempt保留独立成本，不覆盖前一条。
5. 主DB账本为事实源，日志是可补偿镜像；跨库通过持久outbox/重放，不能伪称数据库原子事务。

## 精确接入点

- 同步controller/relay.go逐次getChannel之后、relayHandler发起前：记录attempt，禁止只在成功最后一条日志记录尝试成本。
- 同步service.PostTextConsumeQuota等真实usage归一之后：用已捕获采购价版本算成本，记录attempt，再消费日志引用成本ledger id于admin_info；不向终端用户公开供应商采购价。
- 异步executeTaskSubmissionWith实际渠道选定且发送前：attempt；任务持久后写成本锚点。轮询成功获得真实usage后按原版本，不用当前采购价回算。
- 财务展示revenue/cost/profit：收入来自实际客户记账（消费正、退款负），不能直接把充值或赠送额度当现金收入；成本已知且同币种可减得账面毛利，换汇需不可变依据。

## 配置与体验

- 渠道详情新增采购价，不要求复制/重命名同名模型；不同渠道同一模型可独立进价。
- 客户组/用户优惠在分组定价独立卡片；令牌计费组与渠道可用组是路由/访问，不当采购价配置。
- 管理统计必须显示已知成本覆盖率、未知任务/尝试数、账面毛利口径；未知利润显示未知，不填0。
- 历史无采购价版本的任务保持未知，禁止按今日价格回填。

## 仍需要真实合同信息

各上游精确模型ID、采购计量单位/分辨率音画条件、采购价/折扣/币种、生效时间；失败和重试是否收费；上游账单/usage来源和汇率依据。不得根据客户售价猜采购价。

## 验收

SQLite/Postgres并发、同请求跨渠道重试、发送后超时、免费0进价与未知区分、价格变更后旧任务按旧版、重复回调/轮询、跨库日志失败重放、decimal舍入与币种，均需测试。任何基础设施或真实协议未验证项单列，不能以build通过当成本结算正确。
