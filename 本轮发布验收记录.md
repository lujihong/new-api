# 鑫元宝 AICC 与 Seedance 修复发布验收（2026-09-16）

## 计费全链路收口与价格核验 rc37-20260918-r5（已发布）
- **Grok 视频计费闭环**：排查证实旧任务执行路径仅向表达式注入 `Usage facts`，缺少请求体 `Body`，导致 `param("resolution")` 恒为空并恒走末尾 720p 分支。现引入白名单请求参数快照 `TaskRequestParams`，冻结 `resolution`、`resolution_name`、`size`、`seconds`、`duration`、`aspect_ratio`，并在完成结算时恢复冻结参数；将兼容字段 `resolution_name` 规范化映射至 `resolution`；严禁将完整请求体、prompt、URL 或 Token 记入账单快照；表达式金额按秒乘以 QPU，无除以百万错误。
- **OpenAI Images 用量归一化闭环**：排查证实标准 Images 响应中的 `output_tokens` 仅进入通用 `completion_tokens`（`c`），导致生产 `img_o` 表达式漏算图像输出费用。现修改 Images 专用归一化逻辑，通过原始响应 JSON 判断 `completion_tokens_details` 存在性，仅缺失时映射 `output_tokens` 至 `img_o`，显式零值与混合分项保持原义；当成功响应缺失用量时标记 `usage_missing` 保守保留预扣，不伪造 1 token 掩盖事实。
- **采购成本与结算事务硬化**：采购价格版本 `purchase_price_versions` 实施不可变契约，禁止发布后修改价格、币种或单位，仅支持显式废弃操作，增加 `unit_price_decimal` 精确字段与标量非负校验；上游尝试记录 `upstream_attempts` 增加 `cost_amount_decimal` 与严格的单向状态机，仅允许 unknown 到 known 流转一次，相同值幂等，不同值阻断报错；结算事务 `SettleTaskQuotaTransactional` 严禁无 DB 任务时凭空动账，严格校验 ID 与 TaskID 唯一性、令牌/订阅/用户归属与更新行数；新增主库 `billing_operations` 表作为结算收据与重试操作依据。
- **并发日志轮转修复**：修复 `logger/logger.go` 中原子布尔调度门闩与所有权释放逻辑，仅实际抢占成功的协程执行轮转并释放门闩，消灭并发轮转下的数据竞争与写器状态异常。
- **前端价格展示与逐模型折扣重构**：模型卡片采用独立只读逐秒投影解析视频模型表达式，严禁将 `vs` 混入 token 变量；图片模型补齐输入输出缓存完整分项；逐模型折扣卡片支持原行比例编辑、免费规则专门确认弹窗、会话 `sid` 隔离与晚返回防护、万条规则分页渲染限制（每页 25 条）与筛选空态提示；修正“9.5折”、“8.5折”与“沿用组价”文本。
- **官方定价核验与证据固化**：
  - xAI 官方页面（2026-09-14）明确输出按秒价格（USD）：`grok-imagine-video` 480p $0.05/s，720p $0.07/s；`1.5` 480p $0.08/s，720p $0.14/s，1080p $0.25/s，以及媒体输入费用。
  - OpenAI 官方页面（2026-09-09）明确 GPT Image 2 与 2.5 共享图像输出费率 30 USD/百万 token，并给出质量/尺寸单张示例。本站人民币零售定价保持有意商业策略，不因官方美元价格擅自重算。
- **验证与部署**：
  - Go 单元测试：`model`、`logger`、`relay/channel/openai`、`relay/channel/task/jsplugin`、`pkg/billingexpr`、`service`、`relay` 全部 PASS；核心测试通过 `-race` 竞态检测，零数据竞争。
  - 前端全量测试：修复 `PasskeyDomainSelector` 测试 Provider、`setup-guide` 异步时序、`sidebar-config` 真实素材项对齐、`editor-layout` 按钮匹配与 `detail-preview` pricing 缓存键名后，全部测试 100% 通过；`tsgo -b` 零错误；`oxlint` 零错误零警告；`rsbuild` 生产构建成功。
  - 生产备份：发布前完成数据库备份 `/data/xybcloud/backups/release-20260918/newapi.dump`（466 TOC entries 校验通过）。
  - 镜像构建：通过远端受限构建器构建 `xyb-new-api:rc37-20260918-r5`（manifest digest: `sha256:d7eab0046b98b85557a1492e19142cd697cc3219d149c16b1ca22ca08cde58c8`）；构建完成后已停止构建器，内存 1905MB 可用，0 swap。
  - 生产发布：通过全新发布清单 `compose-rc37-r5-workbench-r7.json` 部署，仅替换网关服务，工作台 r7 保持无缝运行；发布后容器零重启（RestartCount=0），两站 200 OK，新迁移表结构校验完备，版本头信息显示 `v1.0.0-rc.37`。代码已精确提交并推送到 GitHub `main` 分支（commit: `be7be4efb`）。
- api.xybcloud.com：镜像 xyb-new-api:rc37-20260917-r3，本地 /Users/lujihong/docker/www/鑫元宝/new-api。
- www.xybcloud.com：镜像 xyb-workbench:discount-20260917-r7，本地 /Users/lujihong/docker/www/鑫元宝/infinite-canvas。
- 浪起未修改；infinite-canvas-repo不是本次发布源码。
- 线上当前发布配置 /data/xybcloud/releases/aicc-20260916/compose-rc37-workbench-r7.json，项目名必须 -p xyb-aicc-release。禁止盲用旧compose重建。

## 工作台模型数量对齐与音频模型下拉分类 discount-r7（已发布）
- 修复问题 1（模型数量不一致）：工作台 `availableModels` 之前静态包含了已在网关禁用的 `MiniMax-H3`（原本停留在何总中转站渠道），导致工作台显示“可用 25 个模型”，而中转站模型广场只有 24 个模型。已在工作台后端建立动态同步机制（从 `FetchModelPricingList` 实时对齐网关在售模型），并在生产数据库中剔除 `MiniMax-H3`，数量精确对齐为 24 个。
- 修复问题 2（音频模型下拉混入文本/视频模型）：排查发现 `ConfigModelInput` 与 `ModelPicker` 在能力过滤结果为空时执行了错误的降级逻辑（`return matched.length > 0 ? matched : allModels`），导致音频下拉列表将视频模型（MiniMax-H3）和文本模型（Claude 等）全部倒灌出来。修复后严格按能力隔离，当通道未配置音频模型时，自动提供常用/推荐音频模型（gpt-4o-mini-tts, tts-1, tts-1-hd, glm-tts）并标注“推荐语音”，支持自定义模型输入，彻底杜绝跨类型模型混淆。
- 验证与上线：单元测试（42 项全通过，含能力严格隔离用例）、`tsc` 零错误、PostgreSQL 数据库更新、镜像通过编译发布，两站 200 OK 零重启。

## 渠道采购成本版本与上游尝试账本 rc37-r3（已发布）
- 建立独立的采购成本版本库 `purchase_price_versions` 与上游调用尝试账本 `upstream_attempts`：将渠道进价（Cost）与客户零售价（Price）、客户折扣（Discount）彻底分层，不从客户消费账单倒推成本。
- 严格遵循已知成本（known）与未知成本（unknown）分离设计：未签订采购合同的渠道或模型上游调用标记为 `unknown`、金额 `null`，禁止按客户售价或今日单价虚构/伪造采购价。
- 异步任务终态成本记录：在轮询完成阶段根据任务创建时刻匹配有效采购合同版本，持久化权威上游调用记录与成本事实，支持重试/多尝试独立核算。
- 生产数据库迁移与核验：两表已由生产 PostgreSQL 启动自动迁移创建并通过索引结构验证；发布前完成数据库备份 `/data/xybcloud/backups/rc37-r3-20260917/newapi.dump`；通过 `compose-rc37-r3.json` 部署，两站 200 OK 零重启。

## 退款事务原子性与单事务结算 rc37-r2（已发布）
- 重构异步任务退款（RefundTaskQuota）与差额结算（RecalculateTaskQuota）底层状态机：将任务配额、用户余额（钱包/订阅）、令牌额度、用户与渠道已用用量及日志记录约束至单事务 `SettleTaskQuotaTransactional`。
- 彻底消除重复退款隐患：通过 `lockForUpdate` 严格互斥加锁，以数据库持久化权威配额计算真实 delta；当任务已结算为 0 或目标额度时，后续重复调用或轮询重试完全空转并返回 true，杜绝重复退款与资金击穿。
- 保证崩溃一致性：若持久化或资金更新遭遇中断或回写失败，事务全量回滚，不会产生“资金已退还但任务 quota 仍残留”的孤儿状态。
- 测试验证与上线：专项幂等性测试、20 协程高并发退款竞态测试、差额结算并发测试全数 PASS；`logger` 数据竞争通过 `atomic.Int64` 消除；通过 `compose-rc37-r2.json` 完成线上无缝发布，两站 200 OK 零重启。

## 安全合并升级 New API v1.0.0-rc.37（已发布）
- 官方差异安全合入：合入上游 v1.0.0-rc.37（429 个文件修改，4.1 万行变动），吸收多域名 Passkey、定价表达式草稿转换、固定按次与时间条件定价、图片用量/缓存、渠道创建与编辑统一（任务插件参与提供商/模型发现）、阿里万相/Sora 协议更新以及 `/api/status` 前端去重。
- 现有业务资产 100% 完整保留（零丢失）：
  1. AICC 完整套件（本地上传、OpenAPI 签名、本人隔离）；
  2. 逐模型客户优惠（ModelDiscountRules 条件 CAS 保存、个人折后报价 `/api/pricing/self`）；
  3. 异步任务计费事实落库与防时间漂移准备结算（PreparedTieredSettlement）；
  4. 今日上线的 8 类异步任务渠道测试跳过（`status: "skipped"`）与巡检防误报；
  5. 站点定制与品牌（LaunchAI、无外链、高对比度、footer_html、site-overlay 覆盖文件）。
- 验证与部署：Go controller/service/pkg/ratio_setting 关键用例全数通过；前端 Vitest 23 项通过；`tsgo -b` 零错误通过；生产数据库备份至 `/data/xybcloud/backups/rc37-upgrade-20260917/newapi.dump`（431 TOC entries 校验）；滚动更新后容器 0 重启，两站 200 OK，版本实测显示 `v1.0.0-rc.37`，本人报价与渠道跳过逻辑经实测确认完好无损。

## 渠道测试跳过与分组说明 channel-test-r1（已发布）
- 修复用户截图中的报错根因：后端对8类异步任务渠道（包含 DoubaoVideo、Midjourney、Kling、Jimeng、Vidu、TaskPlugin 等）由本地控制器直接拒绝，未向移动云或上游发起请求。
- 修复了此前自动巡检将此类跳过通道误计为成功并覆盖响应时间的重大缺陷：后端新增结构化 `status: "skipped"`、`error_code: "channel_test_unsupported"`，明确标注未请求上游、未发起生成，真实可用性须走生成链路；自动巡检单独计 Skipped，不覆盖延迟、不触发误启误禁，其他真实 localErr 严格计失败。
- 前端测试动作层与弹窗联动修复：识别 `status: "skipped"` 给出中性提示，不在列表更新伪响应时间；批量测试单独计跳过数，跳过模型严格排除在“删除失败模型”候选名单外；弹窗对异步渠道提供清晰免责提示。
- 分组与路由指南修正：在分组配置页补全渠道多分组、路由组基础倍率与组间特殊倍率替代关系、逐模型客户优惠叠加规则、auto按权限顺序选路而非低价优化、以及真人素材账号兼容边界。
- 验证与部署：本地及容器内 Go 控制器测试 16 组全覆盖通过；前端 Vitest 29 项全部通过；tsgo 类型检查零错误；oxlint 零错误零警告。发布前完成 postgres `newapi` 数据库备份 /data/xybcloud/backups/channel-test-20260917/newapi.dump；使用 compose-channel-test-r1.json 部署，两站健康检查 200 OK，零重启，未影响线上任何实际价格与渠道优先级。

## 最终用量落库usage-r3（已发布）
- 独立最终审查0b56 PASS：真实build/vet/定向race通过；主会话最终关键用例count3通过。发布后两站200/零重启，构建器停止、Swap0，原回滚容器保留。未补算历史任务。
- 单/批轮询成功终态CAS前复制计费上下文与usage map，实际用量覆盖估算，保存匹配档位；CAS赢家之后才扣费。等额/零用量亦落真实facts，不回填历史任务。
- 初版双求值被独立审查指出动态时段可能跨边界；已改preparedTieredSettlement复用一次result+err，测试准备后改坏表达式/usage仍按准备结果结算通过。
- 全插件回归发现AICC对全请求JSON marshal把NaN用量误403，已改decoded tree遍历+typed局部normalize。独立探针metadata *string/RawMessage字符串旧行为差异，已补独立any归一，新增本人与外人用例；depth128/nodes100000拒循环/超深。
- 主会话service/jsplugin/billingexpr全包、相关race、build./.../vet通过；远端usage-final-test/build通过，usage-20260917-r3候选哈希已核对一致。终审0b56完成PASS，旧FAIL发现均逐项修复回归后发布。
- 发布前备份/data/xybcloud/backups/usage-20260917，pg_restore目录验证通过。
- 退款幂等/终态与资金崩溃一致性、成本利润账本仍不在此补丁完成范围。

## 金额展示r6最终发布
- 工作台discount-20260917-r6发布，健康200/零重启，网关discount-r1不变；构建器停、两个临时交换文件swapoff删除。
- 最终金额语义后端全service/handler/router与vet/race通过，远端再回归通过；前端组合39项+TS通过。parseErrorMessage旧测试的无条件全退款期望改为只述原因，不保证未证实退款。
- 线上钱包→流水明细→任务与消费记录，退款显示额度退还/具体表达式结算原因，不再前端全失败全额；Seedance按实际用量。证据artifact://0f27cd0e-13f2-4e77-a904-1b7fbb010228。未再生成收费任务，不宣称全量账单逐条对齐。

## 金额展示候选历史（以下以最终发布记录为准）
- 实读充值Money×QuotaPerUnit、wallet quota/QuotaPerUnit，修日志额外×10，使同1000quota钱包/日志均¥0.002/0.02积分。生产只读聚合有21条正小额(<250quota)消费受round误免费影响，未修改历史quota。
- 后端按原始quota判免费，失败有扣费保留金额；成功任务(completed/succeeded/success)退款标差额退还，未知仅额度退还；小额退款保留非零表示。
- 主会话发现前端isFailed硬写0/退款全退及双+会覆盖后端，已改consumptionCostPresentation并3项测试。严格读取网关公开货币配置显示CNY（失败不猜汇率），基价USD保留。
- 工作台整体service/handler/router+vet通过、相关race通过，完整HTTP钱包/日志同额验证通过；前端统一39项+TS通过。独立backend审查通过，前端二次verification调用失败No assistant messages found不计通过，主会话直接Read/测试核验。
- 网关扩大四包回归controller首跑失败未提取用例，后续release/default各887测试通过，日志controller-regression-results.jsonl与controller-debug-regression-results.jsonl保留。不能抹去首次失败或宣称已归因。
- 候选工作台discount-20260917-r5，任务bo6x9tuho远端构建已Compiled successfully，待成镜像；期间专用swap2+4GiB已启用，结束须stop builder/swapoff清理。

## discount-r1 / 工作台discount-r3最终验收
- 优惠规则内核、条件保存CAS、精确模型目录、本人报价已发布，生产规则仍为空，未改任何真实客户优惠或模型基价。
- 最终网关TestModelDiscount/TaskSnapshot/AICCUpload跨setting/model/controller/helper/service全部通过；工作台service/handler/router完整回归通过（真实JWT两个路径报价、匿名401）。主会话报价前端139项+增补11项、工作台本人报价8项及TS通过。
- 独立审查发现旧草稿覆盖、自定义模型目录缺项、负零canonical冲突，均已修并补测试；工作台缺生成区订阅、公开路径漏UserAuth由主会话线上发现并修。r1/r2报价不可用不当通过，r3才闭合此接口。
- 4个实际绑定账号server-side只读/pricing/self返回200/success/24模型/24本人倍率/private no-store，未输出凭据。
- 工作台r3线上Seedance标签为按实际用量结算，artifact://9be943d4-e9fd-4a10-b361-0110cd5e83d0；browser未捕获请求却推测RSC原因不采用。服务端实际GET /api/model-pricing两次200(8.49ms/4.33ms)补足请求发生证据。
- 管理卡片与目录只读线上artifact://f748638d-2e96-478d-9f87-9f8a947b4d91；模型广场详情artifact://4823b6ae-4468-47e0-9997-b6bbb27124ec。仅只读，未在线设置优惠再收费验算。
- 双DB备份/data/xybcloud/backups/model-discount-20260916、恢复目录校验通过；两站200/零重启，原回滚镜像配置保留；构建器stop、专用swap撤销，Swap0。
- 待办仍包括真实素材上传入库(工具门禁)、钱包/流水积分换算冲突、工作台USD与CNY展示对齐、渠道采购价/成本利润账本、原有最终facts/幂等退款及全模型官方核价。

## r5/r4发布完成补记
- 远端Docker验证阶段 `go test ./service -run 'Snapshot|Recalculate' -count=1`通过，镜像构建通过后才发布网关r5；工作台r4先独立发布。生产双方200/零重启。
- 网关切换前备份/data/xybcloud/backups/aicc-ui-r5-20260916、pg_restore目录验证通过。配置和业务价格未改。
- 线上工作台分页computed marginTop=12px,gap=12px,alignItems=center证据artifact://e12b903a-fb7c-4aeb-936b-70273f6e7998；报告中“172px间距”不作为精确布局结论，可能选错下拉框。
- 网关新文件按钮/单一状态/隐藏原生input线上观察通过，artifact://76dac864-ec01-4d11-823c-7c4f8a2b948f。实际选文件仍被工具页面对象门禁拒绝，artifact://4a29af27-7146-4121-a9cc-afc242b9b067，不记真实上传入库通过。
- 构建期间130为主动取消旧任务，255为SSH掉线但远端进程继续；后续读取服务器日志确认测试及镜像导入最终成功。
- 独立构建器已stop，专用2GiB swap在可用内存足够时swapoff并删除；未重置本机Docker或删除用户数据。

## 本次候选构建历史（以下未完成表述均为当时状态，以上最终发布记录优先）
- 用户截图文件选择状态矛盾已实证：chooseFile调用clearFile清空native input但保留React File，新增断言修前失败。
- 现改统一文件卡片/名称大小/更换移除，接受拖放时清旧native以支持A→B→A；提交中禁止变更，状态明确区分已选/上传/提交/失败。工作台提交成功清除file/url，防仅改名称重复提交；入库等待期拖放不再静默中断并重交。
- 组/素材分页独立nav，12px按钮间距及上边距、68px页码居中；需最终线上样式验收。
- 主会话最新网关59项、工作台上传及身份14项、双TS、网关两目标文件lint零警告零错误通过。独立初审的问题已补修，非等同最终真机通过。
- 本机磁盘耗尽（Go链接no space、Docker BuildKit只读），停止本机构建，未删用户文件/清Docker。精简源码传至服务器独立目录/data/xybcloud/builds/aicc-ui-20260916-r5，排除.env/数据/日志/依赖；关键4文件SHA256本地远端一致。
- 远端新建xyb-release-limited构建器，CPU1核、物理内存1600MiB，沿用daemon既有镜像源；首次DockerHub超时、Bun next解包一次失败后重试通过，Next仍在构建。因内存顶限加专用2GiB swap文件build.swap及容器swap总量3200MiB；构建结束必须评估停构建器及swapoff（不能在交换使用高时强行释放）。
- 网关候选还含task_billing.go快照重算修复；代码和测试已主读、独立静态无阻塞，agent新专项省调试符号通过。宽匹配远端Dockerfile.verify snapshot-test尚未成功执行（首次连接断开），不允许据此发布计费修改。
- 线上仍r4/r3，两站200/零重启；本轮真实上传至用户新建“测试”组仍被浏览工具页面对象门禁阻塞，artifact://cee514f1-f352-4052-8229-e04a0acc55b2，不绕过不冒称通过。

## 本次r4/r3增量验收
- 所有常规AICC接口管理员也默认本人；显式admin路径仅管理，不在读取时认领。修前两用例失败、修后通过；增加真实JWT/模型token/禁用用户路由矩阵通过。
- 文件上传落盘/data/aicc-uploads，24小时签名、账号/组/类型绑定、配额与定时清理；ffprobe验证真实音视频，生产镜像已实际检查/usr/bin/ffprobe存在。
- 主会话重跑网关57项前端与工作台26项专项通过，双TS与Linux amd64构建通过；工作台Go全handler/service/router通过；网关AICC相关controller/service/router/middleware通过。不等于整个网关全量测试通过。
- 网关service/aicc_test旧fixture依赖管理员旁路，本次改为真实本人归属/session表。router真实鉴权fixture补InitLogDB初始化方言列；复跑通过。工作台新增VM测试补共享Error与跨realm数组比较，26/26通过。
- 上传/工作台子agent因30分钟时限结束，产物由主会话接手读取与验证，不计agent完成即验收。
- 切换前备份 /data/xybcloud/backups/aicc-upload-r4-20260916，双dump恢复目录通过。第一次未传项目名触发容器名冲突，旧服务健康；补显式项目名后切换成功，两站200、零重启，原回滚容器保留。
- 线上视频页直接按钮可见并进入人物Tab：artifact://7716cd66-4c8a-4597-894f-6a53d71d94f1。
- 线上网关默认我的素材，真人/虚拟均0组；显式管理可见9个历史组，未认领：artifact://1adb9fe9-6029-4e33-a809-71bf6d03638a。
- 线上画布工具栏人物按钮与默认人物Tab纠偏检查：artifact://a9310cb3-6a53-43d2-afb6-5988a1352ede。早一轮摘要混入历史画布聊天，不采用作新操作证据。
- 真实上传验收使用400x400无人物合成PNG，尝试新建测试组时浏览工具连续报页面对象改变拒绝填表，已停止，不绕过安全门，不认定新组/素材创建成功。证据artifact://af613b0f-d66e-4c75-acb6-3212bfe811d7、artifact://b583f178-6e62-429c-a529-c2ab68cca87a。

## 故障与恢复证据
任务 task_FReN1hnsYcDRKhR0YyvwmmcFW1gRz2HN，移动云渠道6，SUCCESS且private_data有成片地址。
旧查询结果只有completed无url。新查询已含签名video_url，签名地址下载HTTP206、video/mp4、MP4 ftyp文件头。
此次未重新生成、未额外扣视频生成费用。用户原画布节点是否已自动恢复显示未作为通过项。

## 测试
- 工作台Go handler/service/router测试通过；tsc --noEmit --incremental false通过；Next生产构建与amd64镜像构建通过。
- 工作台视频内容/人物引用14项node测试通过；真实React组件账号切换1项jsdom测试通过。
- 中转站AICC/Doubao Go测试通过；完整go build通过；tsgo清零；素材界面26项测试通过。
- 双数据库备份恢复至断外网隔离网络，两站新镜像启动与迁移健康通过。
- 隔离副本使用两名不同用户，素材和组GET/PUT/DELETE及认证回查共7个实际HTTP请求均返回403。
- 原独立验证发现账号切换loading不释放、AVIF/HEIC误认视频，均已修并新增回归。
- 失败或中止的子agent、启动失败的临时浏览器验证服务器未计为通过。

## 线上UI证据
- 中转站侧栏短名“真人素材”；新建组弹窗可打开取消。
- 素材组分页1→2→1首条数据随之变化。
- 未命名真人素材组正常显示，不再因groupName缺失报格式错误。
- 最终390px视口：clientWidth=scrollWidth=innerWidth=390，无横向溢出。
- 实际内容滚动容器：scrollHeight1128/clientHeight586/scrollTop542，达到底部，分页可到达。
- 工作台/video以及已有/canvas/...中人物素材与认证Tab均实际打开；专属代理素材列表HTTP200。
- UI验证未执行删除、未代替真人活体、未触发收费生成。
- web_browse摘要曾误写不存在的分类、将横向溢出判PASS；主会话纠偏复测后仅采用上述实际数据。

## 生产保护
备份目录 /data/xybcloud/backups/aicc-release-20260916-160408：双数据库dump、当前inspect、compose、原工作台server。
原容器保留：new-api-rollback-aicc-20260916 / infinite-canvas-rollback-aicc-20260916。
切换保留原环境变量、网络xybnet与数据库。Redis启用，两站零重启。
生产用户数中转站4、工作台5，与备份一致。

## 本地目录迁移
同盘完整目录移动，2481个中转站文件、467个工作台文件（含源码及关键配置）哈希与Git HEAD/status一致。
附属new-api-rc36 worktree指针已repair。
迁移后再次通过双站Go测试、中转站go build、两站前端类型检查、中转站26项及工作台15项专项测试。
迁移校验清单保存在父目录 迁移校验记录.json（0600，不提交）。

## 仍保留的边界与未完项
1. 真人本人完整授权、新素材真实入库及用该资产新生成的端到端验收未完成，不能以UI入口通过替代。
2. 文件直传与取消/重试已上线并通过本地专项，线上真实上传入库因浏览工具填表校验阻塞未验收；预览链接自动续期仍需完善。
3. 历史资源root受控恢复API和事务审计已实现测试，未无证据批量认领历史素材，尚缺专用管理UI。
4. 素材持久上游账号scope、所有协议/自定义插件绕过门禁的完整安全审查仍需持续，不宣称绝对无漏洞。
5. 全模型官方价格、渠道成本与利润/客户折扣的完整审计未完成。
6. 本轮多阶段改动未做最终精确Git提交推送，未提交文件已保留。
